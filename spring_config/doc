使用RabbitMq进行刷新形式时，当配置了相关的的注解以及添加了相关的依赖之后，这个项目就相当于已经启动了配置更新的行为
启动两个client，这时修改git服务器中文件，然后向其中的一个客户端发送一个刷新请求就可以自动的同步所有的配置信息。

猜测可能的实现在于，scope会维持一个配置文件的信息，当refresh请求到来时，它自动的刷新了所属区域中的配置文件，然后
因为每一个客户端维护了一个其它微服务的模块，通过bus发送刷新请求到其它所有注册了刷新服务的微服务中。实现配置文件的
更新操作。